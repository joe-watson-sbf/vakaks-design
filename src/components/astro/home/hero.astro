---
type Props = {};

const {} = Astro.props;

interface Frontmatter {
  heading: string;
  btnText: string;
  btnLink: string;
  image: string;
  description?: string;
}

const hero = await Astro.glob<Frontmatter>("./content/hero.md");
const { heading, btnText, btnLink, description } = hero[0].frontmatter;

---

<section id='hero' class='-mb-3xl 2xl:max-w-7xl 2xl:mx-auto h-[50vh] border-special-0 p-4 grid place-items-center bg-primary-light animate-flip-up-2' transition:name="hero">

  <div class="lg:container lg:mx-auto">
    <h1 class='text-4xl font-bold font-bangers capitalize text-font-5 text-center text-primary-blue'>
      { heading }
    </h1>
    <div class="my-16">
      <p class='text-center text-primary-dark/90 text-font--1 animate-name'>
        { description }
      </p>
      <a href={ btnLink } class=''>
        { btnText }
      </a>
    </div>
  </div>
  
</section>
